import { Avatar, IconButton, styled } from '@mui/material';

export const ButtonSocial = styled(IconButton)(() => ({
  maxWidth: '60px',
  maxHeight: '60px',
}));

export const AvatarSocial = styled(Avatar)(() => ({
  backgroundColor: 'transparent',
}));
